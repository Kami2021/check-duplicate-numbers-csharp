# check-duplicate-numbers-csharp
A simple C# console application that checks if a list of integers contains duplicate values. The program accepts user input one number at a time, validates integers, and reports whether duplicates exist. Built for practice in input validation, loops, and basic algorithm design.
